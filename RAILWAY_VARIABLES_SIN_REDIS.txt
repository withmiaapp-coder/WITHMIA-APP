# ✅ VARIABLES SIMPLIFICADAS - SIN REDIS (Deploy Rápido)
# Usa estas variables si quieres desplegar AHORA sin Redis

# === APLICACIÓN ===
APP_NAME=WithMIA
APP_ENV=production
APP_KEY=base64:fW9jZGRkNWE2MTg5NTQyZjFhMzBmNjA4YjQ5YWRlNGQyZDg3MzNiNGFiZDc5MThjZThhOTNlODU3YWE1ZDEx
APP_DEBUG=false
APP_URL=https://app.withmia.com

# === BASE DE DATOS (Railway inyecta automáticamente las variables MYSQL*) ===
DB_CONNECTION=mysql
DB_HOST=${MYSQLHOST}
DB_PORT=${MYSQLPORT}
DB_DATABASE=${MYSQLDATABASE}
DB_USERNAME=${MYSQLUSER}
DB_PASSWORD=${MYSQLPASSWORD}

# === SESSION & CACHE (Usando Database en lugar de Redis) ===
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_DOMAIN=.withmia.com
SESSION_SECURE_COOKIE=true

CACHE_STORE=database
QUEUE_CONNECTION=database

# === BROADCASTING ===
BROADCAST_DRIVER=log
BROADCAST_CONNECTION=log

# === FILESYSTEM ===
FILESYSTEM_DISK=public

# === LOGGING ===
LOG_CHANNEL=stack
LOG_LEVEL=error
LOG_DEPRECATIONS_CHANNEL=null

# === MAIL (actualiza con tus datos reales si los tienes) ===
MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525

# === OTROS ===
MEMCACHED_HOST=127.0.0.1

# ========================================
# NOTA: Railway inyecta automáticamente:
# - MYSQLHOST, MYSQLPORT, MYSQLDATABASE, MYSQLUSER, MYSQLPASSWORD
# - PORT (para tu aplicación)
# 
# Estas variables NO USAN REDIS
# Una vez que todo funcione, puedes agregar Redis
# y cambiar a SESSION_DRIVER=redis, CACHE_STORE=redis
# ========================================
