# ✅ VARIABLES ACTUALIZADAS PARA RAILWAY
# Reemplaza las variables actuales con estas (usa Raw Editor)

# === APLICACIÓN ===
APP_NAME=WithMIA
APP_ENV=production
APP_KEY=base64:fW9jZGRkNWE2MTg5NTQyZjFhMzBmNjA4YjQ5YWRlNGQyZDg3MzNiNGFiZDc5MThjZThhOTNlODU3YWE1ZDEx
APP_DEBUG=false
APP_URL=https://app.withmia.com

# === BASE DE DATOS (Railway inyecta automáticamente las variables MYSQL*) ===
DB_CONNECTION=mysql
DB_HOST=${MYSQLHOST}
DB_PORT=${MYSQLPORT}
DB_DATABASE=${MYSQLDATABASE}
DB_USERNAME=${MYSQLUSER}
DB_PASSWORD=${MYSQLPASSWORD}

# === REDIS (Railway inyecta automáticamente las variables REDIS*) ===
REDIS_HOST=${REDISHOST}
REDIS_PASSWORD=${REDISPASSWORD}
REDIS_PORT=${REDISPORT}
CACHE_STORE=redis
SESSION_DRIVER=redis
QUEUE_CONNECTION=redis

# === SESSION & DOMAIN ===
SESSION_LIFETIME=120
SESSION_DOMAIN=.withmia.com
SESSION_SECURE_COOKIE=true

# === BROADCASTING ===
BROADCAST_DRIVER=log
BROADCAST_CONNECTION=log

# === FILESYSTEM ===
FILESYSTEM_DISK=public

# === LOGGING ===
LOG_CHANNEL=stack
LOG_LEVEL=error
LOG_DEPRECATIONS_CHANNEL=null

# === MAIL (actualiza con tus datos reales) ===
MAIL_MAILER=resend
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=ssl

# === OTROS ===
MEMCACHED_HOST=127.0.0.1

# ========================================
# NOTA: Railway inyecta automáticamente:
# - MYSQLHOST, MYSQLPORT, MYSQLDATABASE, MYSQLUSER, MYSQLPASSWORD
# - REDISHOST, REDISPORT, REDISPASSWORD
# - PORT (para tu aplicación)
# ========================================
