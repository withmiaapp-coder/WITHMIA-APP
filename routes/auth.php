<?php

/*
|--------------------------------------------------------------------------
| Authentication Routes - LIMPIO
|--------------------------------------------------------------------------
|
| WITHMIA usa únicamente Google OAuth para autenticación.
| Todas las rutas tradicionales de Laravel Breeze han sido eliminadas
| ya que no son necesarias para nuestro sistema de autenticación.
|
| La autenticación se maneja en:
| - POST /auth/google (GoogleAuthController)
| - GET /login (archivo estático login.html)
| - GET /logout (web.php)
|
*/

// Este archivo se mantiene para compatibilidad con Laravel
// pero no contiene rutas activas ya que usamos Google OAuth únicamente
