---
import Layout from '@/layouts/Layout.astro';
import { Hero } from '@/components/Hero';
import { ToolsCarousel } from '@/components/ToolsCarousel';
import { Dashboard } from '@/components/Dashboard';
import { Integrations } from '@/components/Integrations';
import { LiveDemo } from '@/components/LiveDemo';
import { Testimonials } from '@/components/Testimonials';

const organizationJsonLd = JSON.stringify({
  "@context": "https://schema.org",
  "@type": "Organization",
  name: "WITHMIA",
  url: "https://withmia.com",
  logo: "https://withmia.com/logos/logo-withmia.png",
  description: "Plataforma de IA conversacional omnicanal para automatizar ventas y atención al cliente.",
  sameAs: [
    "https://www.facebook.com/automatiza.withmia",
    "https://www.instagram.com/automatiza_withmia/",
    "https://www.linkedin.com/company/withmia",
    "https://www.youtube.com/@withyou-withmia",
  ],
  contactPoint: {
    "@type": "ContactPoint",
    contactType: "sales",
    url: "https://withmia.com/contacto",
    availableLanguage: ["es"],
  },
});

const softwareAppJsonLd = JSON.stringify({
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  name: "WITHMIA",
  applicationCategory: "BusinessApplication",
  operatingSystem: "Web",
  url: "https://withmia.com",
  offers: {
    "@type": "Offer",
    price: "0",
    priceCurrency: "USD",
    description: "Plan gratuito disponible. Planes premium desde $18/mes.",
  },
  aggregateRating: {
    "@type": "AggregateRating",
    ratingValue: "4.8",
    ratingCount: "150",
  },
});
---

<Layout path="/" jsonLd={[organizationJsonLd, softwareAppJsonLd]}>

  <!-- Wrapper keeps canvas as SIBLING of astro-island, outside React DOM -->
  <div id="hero-section" style="position:relative">
    <Hero client:load />
    <canvas
      id="hero-particles"
      aria-hidden="true"
      style="position:absolute;top:0;left:0;width:100%;height:100%;pointer-events:none;z-index:2;display:block"
    ></canvas>
  </div>

  <ToolsCarousel client:visible />
  <LiveDemo client:visible />
  <Dashboard client:visible />
  <Integrations client:visible />
  <Testimonials client:visible />

  <!-- Particle animation — canvas is a sibling of React island, never inside it -->
  <script is:inline>
  (function(){
    if(window.matchMedia&&window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;

    var colors=['255,200,60','255,160,40','200,140,255','100,200,255','255,255,255'];
    var particles=[];
    var animId=0;
    var canvas,ctx,wrapper;

    function resize(){
      var w=wrapper.offsetWidth||window.innerWidth;
      var h=wrapper.offsetHeight;
      if(h<50) h=window.innerHeight;
      canvas.width=w;
      canvas.height=h;
      var count=Math.floor((w*h)/6000);
      particles=[];
      for(var i=0;i<count;i++){
        particles.push({
          x:Math.random()*w,
          y:Math.random()*h,
          vx:(Math.random()-0.5)*0.4,
          vy:-Math.random()*0.6-0.1,
          r:Math.random()*2+0.5,
          o:Math.random()*0.6+0.2,
          c:colors[Math.floor(Math.random()*colors.length)]
        });
      }
    }

    function draw(){
      var W=canvas.width,H=canvas.height;
      ctx.clearRect(0,0,W,H);
      for(var i=0;i<particles.length;i++){
        var p=particles[i];
        p.x+=p.vx; p.y+=p.vy;
        p.o+=(Math.random()-0.5)*0.02;
        if(p.o<0.1) p.o=0.1;
        if(p.o>0.8) p.o=0.8;
        if(p.y<-10){p.y=H+10;p.x=Math.random()*W;}
        if(p.x<-10) p.x=W+10;
        if(p.x>W+10) p.x=-10;
        ctx.beginPath();
        ctx.arc(p.x,p.y,p.r,0,6.2832);
        ctx.fillStyle='rgba('+p.c+','+p.o+')';
        ctx.fill();
        ctx.beginPath();
        ctx.arc(p.x,p.y,p.r*3,0,6.2832);
        ctx.fillStyle='rgba('+p.c+','+(p.o*0.15)+')';
        ctx.fill();
      }
      animId=requestAnimationFrame(draw);
    }

    function start(){
      cancelAnimationFrame(animId);
      animId=0;
      resize();
      if(particles.length>0) draw();
    }

    function tryInit(){
      canvas=document.getElementById('hero-particles');
      wrapper=document.getElementById('hero-section');
      if(!canvas||!wrapper){setTimeout(tryInit,150);return;}
      ctx=canvas.getContext('2d');
      if(!ctx) return;
      // Double-rAF to ensure layout is ready after hydration
      requestAnimationFrame(function(){
        requestAnimationFrame(function(){
          start();
          window.addEventListener('resize',start);
        });
      });
    }

    if(document.readyState==='complete'){
      setTimeout(tryInit,100);
    } else {
      window.addEventListener('load',function(){setTimeout(tryInit,100);});
    }
  })();
  </script>
</Layout>
