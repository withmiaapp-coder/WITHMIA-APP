{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.reverb",
    "watchPatterns": [
      "Dockerfile.reverb",
      "start-reverb.sh",
      "app/Broadcasting/**",
      "config/broadcasting.php",
      "config/reverb.php",
      "composer.json",
      "composer.lock"
    ]
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan cache:clear && php artisan reverb:start --host=0.0.0.0 --port=8080",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
